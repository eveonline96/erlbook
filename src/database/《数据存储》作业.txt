作业：

先参考题目中的ets_test.erl和mysql_test.erl，然后写一系列的测试函数，并比较和分析测试结果:
    (以下的读取都表示随机方式读取，测试工具用ptester.erl中的函数)    
1).mysql数据库测试，用题目中提供的类库操作mysql数据库测试以下项目:
        a.建立一个innoDB类型的表，字段设置参见记录row，测试十万次读和写;
        b.建立一个memory类型的表，字段设置参见记录row, 测试十万次读和写;
    2).mnesia测试: 
        a.建立一个普通表，字段设置参见记录row，分别用mnesia的dirty和transaction方式测试百万次读和写;
	b.建立一个内存表，字段设置参见记录row, 分别用mnesia的dirty和transaction方式测试百万次读和写;


3).dets测试:
        a.测试百万条数据的写入性能，字段设置参见记录row;
        b.测试dets:lookup方式的百万次读取;
        c.测试dets:match方式的百万次读取;
        d.测试dets:select方式的百万次读取;
    4).ets测试:
		a.测试百万条数据的写入性能，字段设置参见记录row;
        b.测试dets:lookup方式的百万次读取;
        c.测试dets:match方式的百万次读取;
       d.测试dets:select方式的百万次读取;   
    5).进程字典测试: 使用进程字典存储数据，测试百万次读和	写，字段设置参见记录row;
	a.测试百万条数据的写入(put)性能，字段设置参见记录			row;
        b.测试get方式的百万次读取;
  6).dict测试：使用进程字典存储数据，测试百万次读和写，字段设置参见记录row;
	a.测试百万条数据的写入(dict:store)性能，字段设置参见记录row;
        b.测试dict:find方式的十万次读取;

2.索引和大数据对性能的影响
    1）对ets，dets，mnesia的读取，match，select中包含主键和不包含主键的读取速度对比
    2）对mysql中包含主键和不包含主键的读取速度对比
    3）对所有存储方式，插入大数据（Key—Value中的Value含有10万元素以上）和读取大数据，和读取小数据的对比
    

3.对以上数据整理成报告，对比各种数据存储方式的利弊，说明各种方式的适用环境。


提交时间：3月23日（周五）18:00前